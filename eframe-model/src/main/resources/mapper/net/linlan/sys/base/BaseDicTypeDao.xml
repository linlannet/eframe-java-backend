<?xml version="1.0" encoding="UTF-8"?>
<!--

    eframe-model - model模型数据模块
    Copyright © 2020-2025 Linlan (open@linlan.net)

    This program is free software: you can redistribute it and/or modify
    it under the terms of the GNU Affero General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    This program is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU Affero General Public License for more details.

    You should have received a copy of the GNU Affero General Public License
    along with this program.  If not, see <http://www.gnu.org/licenses/>.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.linlan.sys.base.dao.BaseDicTypeDao">

    <!-- 采用别名使用类成员变量，可根据需求，确定是否启用 -->
    <resultMap type="net.linlan.sys.base.entity.BaseDicType" id="baseDicTypeMap">
        <id property="id" column="TYPE_ID"/>
        <result property="name" column="NAME"/>
        <result property="typeClass" column="TYPE_CLASS"/>
        <result property="typeRef" column="TYPE_REF"/>
        <result property="typeMode" column="TYPE_MODE"/>
        <result property="searchCode" column="SEARCH_CODE"/>
        <result property="priority" column="PRIORITY"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="lastTime" column="LAST_TIME"/>
        <result property="status" column="STATUS"/>
        <result property="delFlag" column="DEL_FLAG"/>
        <result property="deleteTime" column="DELETE_TIME"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="spare1" column="SPARE1"/>
        <result property="spare2" column="SPARE2"/>
    </resultMap>

    <!-- find by id，通过主键获取对象 -->
    <select id="findById" resultMap="baseDicTypeMap">
        SELECT
        TYPE_ID, NAME, TYPE_CLASS, TYPE_REF, TYPE_MODE, SEARCH_CODE, PRIORITY, CREATE_TIME, LAST_TIME, STATUS, DEL_FLAG, DELETE_TIME, DESCRIPTION, SPARE1, SPARE2
        FROM BASE_DIC_TYPE
        WHERE TYPE_ID = #{value}
    </select>

    <!-- get list，通过输入的条件，获取返回的list列表对象，采用相应的分页规则进行分页处理 -->
    <select id="getList" resultMap="baseDicTypeMap">
        SELECT
        B.TYPE_ID, B.NAME, B.TYPE_CLASS, B.TYPE_REF, B.TYPE_MODE, B.SEARCH_CODE, B.PRIORITY, B.CREATE_TIME, B.LAST_TIME, B.STATUS, B.DEL_FLAG, B.DELETE_TIME, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM BASE_DIC_TYPE B
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>
    </select>


    <!-- 通用排序SQL，如果传入参数按照参数排序，如果未传入参数，按照主键排序 -->
    <sql id="getListOrderBy">
        <choose>
            <when test='orderBy != null'>
                ORDER BY TYPE_ID ASC
            </when>
            <otherwise>
                ORDER BY TYPE_ID DESC
            </otherwise>
        </choose>
    </sql>

    <!-- sql commonList 通过输入的条件，获取返回的list列表对象
    -->
    <sql id="commonList">
        <!--  只允许默认的本表的字段查询，查询条件可通过Param定义，默认全部字段，后续根据Param内的定义，可删除不需要的查询字段 -->
        <if test="typeId != null and typeId != ''">
            and B.TYPE_ID = #{typeId}
        </if>
        <if test="q != null and q.trim() != ''">
            and ( B.NAME like concat('%',concat(#{q},'%'))
            or B.SEARCH_CODE like concat('%',concat(#{q},'%'))
            )
        </if>
        <if test="name != null and name != ''">
            and B.NAME like  concat('%',concat(#{name},'%'))
        </if>
        <if test="sname != null and sname != ''">
            and B.TYPE_ID like concat(#{sname},'%')
        </if>
        <if test="typeClass != null and typeClass != ''">
            and B.TYPE_CLASS = #{typeClass}
        </if>
        <if test="typeRef != null and typeRef != ''">
            and B.TYPE_REF = #{typeRef}
        </if>
        <if test="typeMode != null and typeMode != ''">
            and B.TYPE_MODE = #{typeMode}
        </if>
        <if test="searchCode != null and searchCode != ''">
            and B.SEARCH_CODE = #{searchCode}
        </if>
        <if test="status != null and status != ''">
            and B.STATUS = #{status}
        </if>
        <choose>
            <when test="delFlag != null">
                AND B.DEL_FLAG = #{delFlag}
            </when>
            <otherwise>
                AND B.DEL_FLAG = 0
            </otherwise>
        </choose>
        <if test="description != null and description != ''">
            and B.DESCRIPTION = #{description}
        </if>
        <if test="spare1 != null and spare1 != ''">
            and B.SPARE1 = #{spare1}
        </if>
        <if test="spare2 != null and spare2 != ''">
            and B.SPARE2 = #{spare2}
        </if>
        <if test='key != null and key != ""'>
            AND NAME like concat('%',concat(#{key},'%'))
        </if>
        <if test='existKey != null and existKey !=""'>
            AND (TYPE_ID = #{existKey}
            or NAME = #{existKey}
            or TYPE_ID in (select TYPE_CODE from BASE_DICTIONARY where TYPE_CODE = #{existKey}))
        </if>

        <!-- 各个数据库差异化的查询条件处理，如时间字段 -->
        <choose>
            <when test='_databaseId == "mysql"'>

            </when>
            <when test='_databaseId == "dm8" or _databaseId == "xg"'>

            </when>
            <otherwise>

            </otherwise>
        </choose>

    </sql>

    <!-- save object，保存对象信息 -->
    <insert id="save" parameterType="net.linlan.sys.base.entity.BaseDicType">
        INSERT INTO BASE_DIC_TYPE
        (
            TYPE_ID, NAME, TYPE_CLASS, TYPE_REF, TYPE_MODE, SEARCH_CODE, PRIORITY, CREATE_TIME, LAST_TIME, STATUS, DEL_FLAG, DELETE_TIME, DESCRIPTION, SPARE1, SPARE2
        )
        VALUES
        (
            #{id}, #{name}, #{typeClass}, #{typeRef}, #{typeMode}, #{searchCode}, #{priority}, #{createTime}, #{lastTime}, #{status}, #{delFlag}, #{deleteTime}, #{description}, #{spare1}, #{spare2}
        )
    </insert>

    <insert id="batchSave" parameterType="java.util.List">
        <choose>
            <when test='_databaseId == "mysql"'>
                INSERT INTO BASE_DIC_TYPE
                (
                TYPE_ID, NAME, TYPE_CLASS, TYPE_REF, TYPE_MODE, SEARCH_CODE, PRIORITY, CREATE_TIME, LAST_TIME, STATUS, DEL_FLAG, DELETE_TIME, DESCRIPTION, SPARE1, SPARE2
                )
                VALUES
                <foreach collection="list" index="index" item="item" separator=",">
                    (
                    #{item.id}, #{item.name}, #{item.typeClass}, #{item.typeRef}, #{item.typeMode}, #{item.searchCode}, #{item.priority}, #{item.createTime}, #{item.lastTime}, #{item.status}, #{item.delFlag}, #{item.deleteTime}, #{item.description}, #{item.spare1}, #{item.spare2}
                    )
                </foreach>
            </when>
            <otherwise>
                <foreach collection="list" item="item" index="index" open="begin" close=";end;" separator=";">
                    INSERT INTO BASE_DIC_TYPE
                    (
                    TYPE_ID, NAME, TYPE_CLASS, TYPE_REF, TYPE_MODE, SEARCH_CODE, PRIORITY, CREATE_TIME, LAST_TIME, STATUS, DEL_FLAG, DELETE_TIME, DESCRIPTION, SPARE1, SPARE2
                    )
                    VALUES
                    (
                    #{item.id}, #{item.name}, #{item.typeClass}, #{item.typeRef}, #{item.typeMode}, #{item.searchCode}, #{item.priority}, #{item.createTime}, #{item.lastTime}, #{item.status}, #{item.delFlag}, #{item.deleteTime}, #{item.description}, #{item.spare1}, #{item.spare2}
                    )
                </foreach>
            </otherwise>
        </choose>
    </insert>

    <!-- update object，更新对象信息 -->
    <update id="update" parameterType="net.linlan.sys.base.entity.BaseDicType">
        UPDATE BASE_DIC_TYPE
        <set>
            <if test="name != null">NAME = #{name}, </if>
            <if test="typeClass != null">TYPE_CLASS = #{typeClass}, </if>
            <if test="typeRef != null">TYPE_REF = #{typeRef}, </if>
            <if test="typeMode != null">TYPE_MODE = #{typeMode}, </if>
            <if test="searchCode != null">SEARCH_CODE = #{searchCode}, </if>
            <if test="priority != null">PRIORITY = #{priority}, </if>
            <if test="createTime != null">CREATE_TIME = #{createTime}, </if>
            <if test="lastTime != null">LAST_TIME = #{lastTime}, </if>
            <if test="status != null">STATUS = #{status}, </if>
            <if test="delFlag != null">DEL_FLAG = #{delFlag}, </if>
            <if test="deleteTime != null">DELETE_TIME = #{deleteTime}, </if>
            <if test="description != null">DESCRIPTION = #{description}, </if>
            <if test="spare1 != null">SPARE1 = #{spare1}, </if>
            <if test="spare2 != null">SPARE2 = #{spare2}</if>
        </set>
        WHERE TYPE_ID = #{id}
    </update>

    <!-- delete by id，通过主键删除对象 -->
    <update id="deleteById">
        <choose>
            <when test='_databaseId == "mysql"'>
                update BASE_DIC_TYPE set DEL_FLAG = 1, DELETE_TIME = sysdate(), LAST_TIME = sysdate() WHERE TYPE_ID = #{value}
            </when>
            <when test='_databaseId == "dm8" or _databaseId == "xg"'>
                update BASE_DIC_TYPE set DEL_FLAG = 1, DELETE_TIME = sysdate(), LAST_TIME = sysdate() WHERE TYPE_ID = #{value}
            </when>
            <otherwise>
                update BASE_DIC_TYPE set DEL_FLAG = 1, DELETE_TIME = sysdate(), LAST_TIME = sysdate() WHERE TYPE_ID = #{value}
            </otherwise>
        </choose>
    </update>

    <!-- delete by ids，通过主键数组批量删除对象 -->
    <update id="deleteByIds">
        <choose>
            <when test='_databaseId == "mysql"'>
                update BASE_DIC_TYPE set DEL_FLAG = 1, DELETE_TIME = sysdate(), LAST_TIME = sysdate()
                WHERE TYPE_ID IN
                <foreach item="id" collection="array" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </when>
            <when test='_databaseId == "dm8" or _databaseId == "xg"'>
                update BASE_DIC_TYPE set DEL_FLAG = 1, DELETE_TIME = sysdate(), LAST_TIME = sysdate()
                WHERE TYPE_ID IN
                <foreach item="id" collection="array" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </when>
            <otherwise>
                update BASE_DIC_TYPE set DEL_FLAG = 1, DELETE_TIME = sysdate(), LAST_TIME = sysdate()
                WHERE TYPE_ID IN
                <foreach item="id" collection="array" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </otherwise>
        </choose>
    </update>

    <!-- query total，通过输入的条件，查询返回结果的数量，返回值为int -->
    <select id="getCount" resultType="int">
        SELECT COUNT(1)
        FROM BASE_DIC_TYPE B
        WHERE 1 = 1
        <include refid="commonList"></include>
    </select>

    <!-- 以下为新增特定的操作 -->
    <!-- get list with dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <resultMap type="net.linlan.sys.base.dto.BaseDicTypeDto" extends="baseDicTypeMap" id="baseDicTypeDtoMap">

    </resultMap>

    <!-- get page with param input dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <select id="getPageDto" resultMap="baseDicTypeDtoMap">
        SELECT
        B.TYPE_ID, B.NAME, B.TYPE_CLASS, B.TYPE_REF, B.TYPE_MODE, B.SEARCH_CODE, B.PRIORITY, B.CREATE_TIME, B.LAST_TIME, B.STATUS, B.DEL_FLAG, B.DELETE_TIME, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM BASE_DIC_TYPE B
        WHERE 1=1
        <if test="key != null and key != ''">
            and B.TYPE_ID LIKE CONCAT(#{key},'%')
        </if>
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>

    </select>

    <!-- get dto with input id，通过ID，查询dto对象 -->
    <select id="getDtoById" resultMap="baseDicTypeDtoMap">
        SELECT
        TYPE_ID, NAME, TYPE_CLASS, TYPE_REF, TYPE_MODE, SEARCH_CODE, PRIORITY, CREATE_TIME, LAST_TIME, STATUS, DEL_FLAG, DELETE_TIME, DESCRIPTION, SPARE1, SPARE2
        FROM BASE_DIC_TYPE
        WHERE TYPE_ID = #{value}
    </select>


</mapper>
