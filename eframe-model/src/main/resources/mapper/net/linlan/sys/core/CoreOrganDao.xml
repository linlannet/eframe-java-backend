<?xml version="1.0" encoding="UTF-8"?>
<!--

    eframe-model - model模型数据模块
    Copyright © 2020-2025 Linlan (open@linlan.net)

    This program is free software: you can redistribute it and/or modify
    it under the terms of the GNU Affero General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    This program is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU Affero General Public License for more details.

    You should have received a copy of the GNU Affero General Public License
    along with this program.  If not, see <http://www.gnu.org/licenses/>.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.linlan.sys.core.dao.CoreOrganDao">

    <!-- 采用别名使用类成员变量，可根据需求，确定是否启用 -->
    <resultMap type="net.linlan.sys.core.entity.CoreOrgan" id="coreOrganMap">
        <id property="id" column="SYSORGAN_ID"/>
        <result property="orgtypeId" column="ORGTYPE_ID"/>
        <result property="organOcode" column="ORGAN_OCODE"/>
        <result property="organGroup" column="ORGAN_GROUP"/>
        <result property="organLevel" column="ORGAN_LEVEL"/>
        <result property="approvalNumber" column="APPROVAL_NUMBER"/>
        <result property="isStationOrg" column="IS_STATION_ORG"/>
        <result property="organType" column="ORGAN_TYPE"/>
        <result property="organProperty" column="ORGAN_PROPERTY"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="creatorId" column="CREATOR_ID"/>
        <result property="updatorId" column="UPDATOR_ID"/>
        <result property="updateTime" column="UPDATE_TIME"/>
        <result property="isAdmin" column="IS_ADMIN"/>
        <result property="isMember" column="IS_MEMBER"/>
        <result property="isDisabled" column="IS_DISABLED"/>
        <result property="isVirtual" column="IS_VIRTUAL"/>
        <result property="isPub" column="IS_PUB"/>
        <result property="isGridOrgan" column="IS_GRID_ORGAN"/>
        <result property="isVillageOrgan" column="IS_VILLAGE_ORGAN"/>
        <result property="isPartyOrgan" column="IS_PARTY_ORGAN"/>
        <result property="priority" column="PRIORITY"/>
        <result property="lastTime" column="LAST_TIME"/>
        <result property="delFlag" column="DEL_FLAG"/>
                <result property="orgtypeName" column="orgtypeName"/>
        <result property="organLevelName" column="organLevelName"/>
    </resultMap>

    <!-- 增加扩展类的SysOrgan对象 -->
    <resultMap type="net.linlan.sys.core.entity.CoreOrgan" id="coreOrganMoreMap" extends="coreOrganMap">
        <association property="baseOrgan" column="{value=SYSORGAN_ID}" select="net.linlan.sys.base.dao.BaseOrganDao.findById"></association>
    </resultMap>

    <!-- find by id，通过主键获取对象 -->
    <select id="findById" resultMap="coreOrganMoreMap">
        SELECT
        B.SYSORGAN_ID, B.ORGTYPE_ID, B.ORGAN_OCODE, B.ORGAN_GROUP, B.ORGAN_LEVEL, B.APPROVAL_NUMBER, B.IS_STATION_ORG, B.ORGAN_TYPE, B.ORGAN_PROPERTY, B.CREATE_TIME, B.CREATOR_ID, B.UPDATOR_ID, B.UPDATE_TIME, B.IS_ADMIN, B.IS_MEMBER, B.IS_DISABLED, B.IS_VIRTUAL, B.IS_PUB, B.IS_GRID_ORGAN, B.IS_VILLAGE_ORGAN, B.IS_PARTY_ORGAN, B.PRIORITY, B.LAST_TIME, B.DEL_FLAG
        , (select NAME from CORE_ORGTYPE WHERE ORGTYPE_ID = B.ORGTYPE_ID) as orgtypeName
        , (select NAME from BASE_AREATYPE  WHERE AREATYPE_ID = B.ORGAN_LEVEL) as organLevelName
        FROM CORE_ORGAN B
        WHERE SYSORGAN_ID = #{value}
    </select>

    <!-- get list，通过输入的条件，获取返回的list列表对象，采用相应的分页规则进行分页处理 -->
    <select id="getList" resultMap="coreOrganMap">
        SELECT
        B.SYSORGAN_ID, B.ORGTYPE_ID, B.ORGAN_OCODE, B.ORGAN_GROUP, B.ORGAN_LEVEL, B.APPROVAL_NUMBER, B.IS_STATION_ORG, B.ORGAN_TYPE, B.ORGAN_PROPERTY, B.CREATE_TIME, B.CREATOR_ID, B.UPDATOR_ID, B.UPDATE_TIME, B.IS_ADMIN, B.IS_MEMBER, B.IS_DISABLED, B.IS_VIRTUAL, B.IS_PUB, B.IS_GRID_ORGAN, B.IS_VILLAGE_ORGAN, B.IS_PARTY_ORGAN, B.PRIORITY, B.LAST_TIME, B.DEL_FLAG
        FROM CORE_ORGAN B
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>
    </select>


    <!-- 通用排序SQL，如果传入参数按照参数排序，如果未传入参数，按照主键排序 -->
    <sql id="getListOrderBy">
        <choose>
            <when test='orderBy != null'>
                ORDER BY SYSORGAN_ID ASC
            </when>
            <otherwise>
                ORDER BY SYSORGAN_ID DESC
            </otherwise>
        </choose>
    </sql>

    <!-- sql commonList 通过输入的条件，获取返回的list列表对象
    -->
    <sql id="commonList">
        <!--  只允许默认的本表的字段查询，查询条件可通过Param定义，默认全部字段，后续根据Param内的定义，可删除不需要的查询字段 -->
        <if test='id != null and id != ""'>
            AND (B.SYSORGAN_ID = #{id} or org.ORGAN_ID =  #{id})
        </if>
        <if test="orgtypeId != null and orgtypeId != ''">
            and B.ORGTYPE_ID = #{orgtypeId}
        </if>
        <if test="organOcode != null and organOcode != ''">
            and B.ORGAN_OCODE = #{organOcode}
        </if>
        <if test="organGroup != null and organGroup != ''">
            and B.ORGAN_GROUP = #{organGroup}
        </if>
        <if test="organLevel != null and organLevel != ''">
            and B.ORGAN_LEVEL = #{organLevel}
        </if>
        <if test="approvalNumber != null and approvalNumber != ''">
            and B.APPROVAL_NUMBER = #{approvalNumber}
        </if>
        <if test="organType != null and organType != ''">
            and B.ORGAN_TYPE = #{organType}
        </if>
        <if test="organProperty != null and organProperty != ''">
            and B.ORGAN_PROPERTY = #{organProperty}
        </if>
        <if test="isAdmin != null and isAdmin != ''">
            and B.IS_ADMIN = #{isAdmin}
        </if>
        <if test="isMember != null and isMember != ''">
            and B.IS_MEMBER = #{isMember}
        </if>
        <if test="isDisabled != null and isDisabled != ''">
            and B.IS_DISABLED = #{isDisabled}
        </if>
        <if test="isVirtual != null and isVirtual != ''">
            and B.IS_VIRTUAL = #{isVirtual}
        </if>
        <if test="isPub != null and isPub != ''">
            and B.IS_PUB = #{isPub}
        </if>
        <if test="isGridOrgan != null and isGridOrgan != ''">
            and B.IS_GRID_ORGAN = #{isGridOrgan}
        </if>
        <if test="isVillageOrgan != null and isVillageOrgan != ''">
            and B.IS_VILLAGE_ORGAN = #{isVillageOrgan}
        </if>
        <if test="isPartyOrgan != null and isPartyOrgan != ''">
            and B.IS_PARTY_ORGAN = #{isPartyOrgan}
        </if>
        <choose>
            <when test="delFlag != null">
                AND B.DEL_FLAG = #{delFlag}
            </when>
            <otherwise>
                AND B.DEL_FLAG = 0
            </otherwise>
        </choose>
        <if test='key != null and key !=""'>
            AND (
            B.ORGAN_LEVEL in (select AREATYPE_ID from BASE_AREATYPE where NAME like concat('%',concat(#{key},'%')))
            OR (org.NAME like concat('%',concat(#{key},'%')))
            )
        </if>
        <if test='q != null and q != ""'>
            AND B.CREATOR_ID IN (SELECT bu.USER_ID
            FROM BASE_USER bu where
            bu.USERNAME like concat('%',concat(#{key},'%'))
            or bu.MOBILE like concat('%',concat(#{key},'%'))
            or bu.EMAIL like concat('%',concat(#{key},'%')))
        </if>

        <!-- 各个数据库差异化的查询条件处理，如时间字段 -->
        <choose>
            <when test='_databaseId == "mysql"'>

            </when>
            <when test='_databaseId == "dm8" or _databaseId == "xg"'>

            </when>
            <otherwise>

            </otherwise>
        </choose>

    </sql>

    <!-- save object，保存对象信息 -->
    <insert id="save" parameterType="net.linlan.sys.core.entity.CoreOrgan">
        INSERT INTO CORE_ORGAN
        (
        SYSORGAN_ID, ORGTYPE_ID, ORGAN_OCODE, ORGAN_GROUP, ORGAN_LEVEL, APPROVAL_NUMBER, IS_STATION_ORG, ORGAN_TYPE, ORGAN_PROPERTY, CREATE_TIME, CREATOR_ID, UPDATOR_ID, UPDATE_TIME, IS_ADMIN, IS_MEMBER, IS_DISABLED, IS_VIRTUAL, IS_PUB, IS_GRID_ORGAN, IS_VILLAGE_ORGAN, IS_PARTY_ORGAN, PRIORITY, LAST_TIME, DEL_FLAG
        )
        VALUES
        (
        #{id}, #{orgtypeId}, #{organOcode}, #{organGroup}, #{organLevel}, #{approvalNumber}, #{isStationOrg}, #{organType}, #{organProperty}, #{createTime}, #{creatorId}, #{updatorId}, #{updateTime}, #{isAdmin}, #{isMember}, #{isDisabled}, #{isVirtual}, #{isPub}, #{isGridOrgan}, #{isVillageOrgan}, #{isPartyOrgan}, #{priority}, #{lastTime}, #{delFlag}
        )
    </insert>

    <insert id="batchSave" parameterType="java.util.List">
        <choose>
            <when test='_databaseId == "mysql"'>
                INSERT INTO CORE_ORGAN
                (
                SYSORGAN_ID, ORGTYPE_ID, ORGAN_OCODE, ORGAN_GROUP, ORGAN_LEVEL, APPROVAL_NUMBER, IS_STATION_ORG, ORGAN_TYPE, ORGAN_PROPERTY, CREATE_TIME, CREATOR_ID, UPDATOR_ID, UPDATE_TIME, IS_ADMIN, IS_MEMBER, IS_DISABLED, IS_VIRTUAL, IS_PUB, IS_GRID_ORGAN, IS_VILLAGE_ORGAN, IS_PARTY_ORGAN, PRIORITY, LAST_TIME, DEL_FLAG
                )
                VALUES
                <foreach collection="list" index="index" item="item" separator=",">
                    (
                    #{item.id}, #{item.orgtypeId}, #{item.organOcode}, #{item.organGroup}, #{item.organLevel}, #{item.approvalNumber}, #{item.isStationOrg}, #{item.organType}, #{item.organProperty}, #{item.createTime}, #{item.creatorId}, #{item.updatorId}, #{item.updateTime}, #{item.isAdmin}, #{item.isMember}, #{item.isDisabled}, #{item.isVirtual}, #{item.isPub}, #{item.isGridOrgan}, #{item.isVillageOrgan}, #{item.isPartyOrgan}, #{item.priority}, #{item.lastTime}, #{item.delFlag}
                    )
                </foreach>
            </when>
            <otherwise>
                <foreach collection="list" item="item" index="index" open="begin" close=";end;" separator=";">
                    INSERT INTO CORE_ORGAN
                    (
                    SYSORGAN_ID, ORGTYPE_ID, ORGAN_OCODE, ORGAN_GROUP, ORGAN_LEVEL, APPROVAL_NUMBER, IS_STATION_ORG, ORGAN_TYPE, ORGAN_PROPERTY, CREATE_TIME, CREATOR_ID, UPDATOR_ID, UPDATE_TIME, IS_ADMIN, IS_MEMBER, IS_DISABLED, IS_VIRTUAL, IS_PUB, IS_GRID_ORGAN, IS_VILLAGE_ORGAN, IS_PARTY_ORGAN, PRIORITY, LAST_TIME, DEL_FLAG
                    )
                    VALUES
                    (
                    #{item.id}, #{item.orgtypeId}, #{item.organOcode}, #{item.organGroup}, #{item.organLevel}, #{item.approvalNumber}, #{item.isStationOrg}, #{item.organType}, #{item.organProperty}, #{item.createTime}, #{item.creatorId}, #{item.updatorId}, #{item.updateTime}, #{item.isAdmin}, #{item.isMember}, #{item.isDisabled}, #{item.isVirtual}, #{item.isPub}, #{item.isGridOrgan}, #{item.isVillageOrgan}, #{item.isPartyOrgan}, #{item.priority}, #{item.lastTime}, #{item.delFlag}
                    )
                </foreach>
            </otherwise>
        </choose>
    </insert>

    <!-- update object，更新对象信息 -->
    <update id="update" parameterType="net.linlan.sys.core.entity.CoreOrgan">
        UPDATE CORE_ORGAN
        <set>
            <if test="orgtypeId != null">ORGTYPE_ID = #{orgtypeId}, </if>
            <if test="organOcode != null">ORGAN_OCODE = #{organOcode}, </if>
            <if test="organGroup != null">ORGAN_GROUP = #{organGroup}, </if>
            <if test="organLevel != null">ORGAN_LEVEL = #{organLevel}, </if>
            <if test="approvalNumber != null">APPROVAL_NUMBER = #{approvalNumber}, </if>
            <if test="isStationOrg != null">IS_STATION_ORG = #{isStationOrg}, </if>
            <if test="organType != null">ORGAN_TYPE = #{organType}, </if>
            <if test="organProperty != null">ORGAN_PROPERTY = #{organProperty}, </if>
            <if test="createTime != null">CREATE_TIME = #{createTime}, </if>
            <if test="creatorId != null">CREATOR_ID = #{creatorId}, </if>
            <if test="updatorId != null">UPDATOR_ID = #{updatorId}, </if>
            <if test="updateTime != null">UPDATE_TIME = #{updateTime}, </if>
            <if test="isAdmin != null">IS_ADMIN = #{isAdmin}, </if>
            <if test="isMember != null">IS_MEMBER = #{isMember}, </if>
            <if test="isDisabled != null">IS_DISABLED = #{isDisabled}, </if>
            <if test="isVirtual != null">IS_VIRTUAL = #{isVirtual}, </if>
            <if test="isPub != null">IS_PUB = #{isPub}, </if>
            <if test="isGridOrgan != null">IS_GRID_ORGAN = #{isGridOrgan}, </if>
            <if test="isVillageOrgan != null">IS_VILLAGE_ORGAN = #{isVillageOrgan}, </if>
            <if test="isPartyOrgan != null">IS_PARTY_ORGAN = #{isPartyOrgan}, </if>
            <if test="priority != null">PRIORITY = #{priority}, </if>
            <if test="lastTime != null">LAST_TIME = #{lastTime}, </if>
            <if test="delFlag != null">DEL_FLAG = #{delFlag}</if>
        </set>
        WHERE SYSORGAN_ID = #{id}
    </update>

    <!-- delete by id，通过主键删除对象 -->
    <update id="deleteById">
        <choose>
            <when test='_databaseId == "mysql"'>
                update CORE_ORGAN set DEL_FLAG = 1, LAST_TIME = sysdate() where SYSORGAN_ID = #{value}
            </when>
            <when test='_databaseId == "dm8" or _databaseId == "xg"'>
                update CORE_ORGAN set DEL_FLAG = 1, LAST_TIME = sysdate() where SYSORGAN_ID = #{value}
            </when>
            <otherwise>
                update CORE_ORGAN set DEL_FLAG = 1, LAST_TIME = sysdate() where SYSORGAN_ID = #{value}
            </otherwise>
        </choose>
    </update>

    <!-- delete by ids，通过主键数组批量删除对象 -->
    <update id="deleteByIds">
        <choose>
            <when test='_databaseId == "mysql"'>
                update CORE_ORGAN set DEL_FLAG = 1, LAST_TIME = sysdate()
                WHERE SYSORGAN_ID IN
                <foreach item="id" collection="array" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </when>
            <when test='_databaseId == "dm8" or _databaseId == "xg"'>
                update CORE_ORGAN set DEL_FLAG = 1, LAST_TIME = sysdate()
                WHERE SYSORGAN_ID IN
                <foreach item="id" collection="array" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </when>
            <otherwise>
                update CORE_ORGAN set DEL_FLAG = 1, LAST_TIME = sysdate()
                WHERE SYSORGAN_ID IN
                <foreach item="id" collection="array" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </otherwise>
        </choose>
    </update>

    <!-- query total，通过输入的条件，查询返回结果的数量，返回值为int -->
    <select id="getCount" resultType="int">
        SELECT COUNT(1)
        FROM CORE_ORGAN B
        WHERE 1 = 1
        <include refid="commonList"></include>
    </select>

    <!-- 以下为新增特定的操作 -->
    <!-- get list with dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <resultMap type="net.linlan.sys.core.dto.CoreOrganDto" extends="coreOrganMap" id="coreOrganDtoMap">
        <result property="name" column="name"/>
        <result property="unionCode" column="unionCode"/>
        <result property="organCode" column="organCode"/>

    </resultMap>

    <!-- get page with param input dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <select id="getPageDto" resultMap="coreOrganDtoMap">
        SELECT
        B.SYSORGAN_ID, B.ORGTYPE_ID, B.ORGAN_OCODE, B.ORGAN_GROUP, B.ORGAN_LEVEL, B.APPROVAL_NUMBER, B.IS_STATION_ORG, B.ORGAN_TYPE, B.ORGAN_PROPERTY, B.CREATE_TIME, B.CREATOR_ID, B.UPDATOR_ID, B.UPDATE_TIME, B.IS_ADMIN, B.IS_MEMBER, B.IS_DISABLED, B.IS_VIRTUAL, B.IS_PUB, B.IS_GRID_ORGAN, B.IS_VILLAGE_ORGAN, B.IS_PARTY_ORGAN, B.PRIORITY, B.LAST_TIME, B.DEL_FLAG
        , ORGTYPE.NAME as orgtypeName
        , AREATYPE.NAME as organLevelName
        , org.NAME as name
        , org.UNION_CODE as unionCode
        , org.ORGAN_CODE as organCode
        FROM CORE_ORGAN B
        <include refid="commonUnionJoin"></include>
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>

    </select>

    <!-- 关联表 -->
    <sql id="commonUnionJoin">
        LEFT JOIN BASE_ORGAN org ON org.ORGAN_ID = B.SYSORGAN_ID
        LEFT JOIN CORE_ORGTYPE ORGTYPE ON ORGTYPE.ORGTYPE_ID = B.ORGTYPE_ID
        LEFT JOIN BASE_AREATYPE AREATYPE ON AREATYPE.AREATYPE_ID = B.ORGAN_LEVEL
    </sql>


    <!-- get dto with input id，通过ID，查询dto对象 -->
    <select id="getDtoById" resultMap="coreOrganDtoMap">
        SELECT B.SYSORGAN_ID, B.ORGTYPE_ID, B.ORGAN_OCODE, B.ORGAN_GROUP, B.ORGAN_LEVEL, B.APPROVAL_NUMBER, B.IS_STATION_ORG, B.ORGAN_TYPE, B.ORGAN_PROPERTY, B.CREATE_TIME, B.CREATOR_ID, B.UPDATOR_ID, B.UPDATE_TIME, B.IS_ADMIN, B.IS_MEMBER, B.IS_DISABLED, B.IS_VIRTUAL, B.IS_PUB, B.IS_GRID_ORGAN, B.IS_VILLAGE_ORGAN, B.IS_PARTY_ORGAN, B.PRIORITY, B.LAST_TIME, B.DEL_FLAG
        , ORGTYPE.NAME as orgtypeName
        , AREATYPE.NAME as organLevelName
        , org.NAME as name
        , org.UNION_CODE as unionCode
        , org.ORGAN_CODE as organCode
        FROM CORE_ORGAN B
        <include refid="commonUnionJoin"></include>
        WHERE B.SYSORGAN_ID = #{value}
    </select>

</mapper>
