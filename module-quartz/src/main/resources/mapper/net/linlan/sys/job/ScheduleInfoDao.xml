<?xml version="1.0" encoding="UTF-8"?>
<!--

    module-quartz - quartz定时任务模块
    Copyright © 2020-2025 Linlan (open@linlan.net)

    This program is free software: you can redistribute it and/or modify
    it under the terms of the GNU Affero General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    This program is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU Affero General Public License for more details.

    You should have received a copy of the GNU Affero General Public License
    along with this program.  If not, see <http://www.gnu.org/licenses/>.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.linlan.sys.job.dao.ScheduleInfoDao">

    <!-- 采用别名使用类成员变量，可根据需求，确定是否启用 -->
    <resultMap type="net.linlan.sys.job.entity.ScheduleInfo" id="scheduleInfoMap">
        <id property="id" column="SCHEDULE_ID"/>
        <result property="appId" column="APP_ID"/>
        <result property="jobSource" column="JOB_SOURCE"/>
        <result property="jobType" column="JOB_TYPE"/>
        <result property="scheduleName" column="SCHEDULE_NAME"/>
        <result property="objectName" column="OBJECT_NAME"/>
        <result property="methodName" column="METHOD_NAME"/>
        <result property="params" column="PARAMS"/>
        <result property="cronExpression" column="CRON_EXPRESSION"/>
        <result property="jobConfig" column="JOB_CONFIG"/>
        <result property="startDate" column="START_DATE"/>
        <result property="endDate" column="END_DATE"/>
        <result property="status" column="STATUS"/>
        <result property="creatorId" column="CREATOR_ID"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="lastTime" column="LAST_TIME"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="spare1" column="SPARE1"/>
        <result property="spare2" column="SPARE2"/>
    </resultMap>

    <!-- find by id，通过主键获取对象 -->
    <select id="findById" resultMap="scheduleInfoMap">
        SELECT
        SCHEDULE_ID, APP_ID, JOB_SOURCE, JOB_TYPE, SCHEDULE_NAME, OBJECT_NAME, METHOD_NAME, PARAMS, CRON_EXPRESSION, JOB_CONFIG, START_DATE, END_DATE, STATUS, CREATOR_ID, CREATE_TIME, LAST_TIME, DESCRIPTION, SPARE1, SPARE2
        FROM SCHEDULE_INFO
        WHERE SCHEDULE_ID = #{value}
    </select>

    <!-- get list，通过输入的条件，获取返回的list列表对象，采用相应的分页规则进行分页处理 -->
    <select id="getList" resultMap="scheduleInfoMap">
        SELECT
        B.SCHEDULE_ID, B.APP_ID, B.JOB_SOURCE, B.JOB_TYPE, B.SCHEDULE_NAME, B.OBJECT_NAME, B.METHOD_NAME, B.PARAMS, B.CRON_EXPRESSION, B.JOB_CONFIG, B.START_DATE, B.END_DATE, B.STATUS, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM SCHEDULE_INFO B
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>
    </select>


    <!-- 通用排序SQL，如果传入参数按照参数排序，如果未传入参数，按照主键排序 -->
    <sql id="getListOrderBy">
        <choose>
            <when test='orderBy != null'>
                ORDER BY SCHEDULE_ID ASC
            </when>
            <otherwise>
                ORDER BY SCHEDULE_ID DESC
            </otherwise>
        </choose>
    </sql>

    <!-- sql commonList 通过输入的条件，获取返回的list列表对象
    -->
    <sql id="commonList">
        <!--  只允许默认的本表的字段查询，查询条件可通过Param定义，默认全部字段，后续根据Param内的定义，可删除不需要的查询字段 -->
        <if test="appId != null and appId.trim() != ''">
            and B.APP_ID = #{appId}
        </if>
        <if test="jobSource != null and jobSource.trim() != ''">
            and B.JOB_SOURCE = #{jobSource}
        </if>
        <if test="jobType != null and jobType.trim() != ''">
            and B.JOB_TYPE = #{jobType}
        </if>
        <if test="scheduleName != null and scheduleName != ''">
            and B.SCHEDULE_NAME like concat('%',concat(#{scheduleName},'%'))
        </if>
        <if test="objectName != null and objectName != ''">
            and B.OBJECT_NAME = #{objectName}
        </if>
        <if test="methodName != null and methodName != ''">
            and B.METHOD_NAME = #{methodName}
        </if>
        <if test="cronExpression != null and cronExpression != ''">
            and B.CRON_EXPRESSION = #{cronExpression}
        </if>
        <if test="jobConfig != null and jobConfig != ''">
            and B.JOB_CONFIG = #{jobConfig}
        </if>
        <if test="startDate != null and startDate != ''">
            and B.START_DATE = #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            and B.END_DATE = #{endDate}
        </if>
        <if test="status != null and status != ''">
            and B.STATUS = #{status}
        </if>
        <if test="description != null and description != ''">
            and B.DESCRIPTION = #{description}
        </if>
        <if test="spare1 != null and spare1 != ''">
            and B.SPARE1 = #{spare1}
        </if>
        <if test="spare2 != null and spare2 != ''">
            and B.SPARE2 = #{spare2}
        </if>
        <if test='key != null and key.trim() != ""'>
            and (
            B.SCHEDULE_NAME like concat('%',concat(#{key},'%'))
            or B.OBJECT_NAME like concat('%', concat(#{key},'%'))
            or B.METHOD_NAME like concat('%', concat(#{key},'%'))
            or B.PARAMS like concat('%', concat(#{key},'%'))
            )
        </if>
        <if test="status != null">
            AND STATUS = #{status}
        </if>
        <if test="statusStop != null">
            <![CDATA[ AND STATUS <> 1 ]]>
        </if>

        <!-- 各个数据库差异化的查询条件处理，如时间字段 -->
        <choose>
            <when test='_databaseId == "mysql"'>
                <if test='startTime != null and  startTime.trim() != "" and endTime != null and  endTime.trim() != "" '>
                    <![CDATA[ and date_format(B.CREATE_TIME,'%Y-%m-%d') >= date_format(#{startTime},'%Y-%m-%d')
                and date_format(B.CREATE_TIME,'%Y-%m-%d') <= date_format(#{endTime},'%Y-%m-%d') ]]>
                </if>
                <if test='startLastTime != null and  startLastTime.trim() != "" and endLastTime != null and  endLastTime.trim() != "" '>
                    <![CDATA[ and date_format(B.CREATE_TIME,'%Y-%m-%d') >= date_format(#{startLastTime},'%Y-%m-%d')
                and date_format(B.CREATE_TIME,'%Y-%m-%d') <= date_format(#{endLastTime},'%Y-%m-%d') ]]>
                </if>
            </when>
            <when test='_databaseId == "dm8" or _databaseId == "xg"'>
                <if test='startTime != null and  startTime.trim() != "" and endTime != null and  endTime.trim() != "" '>
                    <![CDATA[ and date_format(B.CREATE_TIME,'%Y-%m-%d') >= date_format(#{startTime},'%Y-%m-%d')
                and date_format(B.CREATE_TIME,'%Y-%m-%d') <= date_format(#{endTime},'%Y-%m-%d') ]]>
                </if>
                <if test='startLastTime != null and  startLastTime.trim() != "" and endLastTime != null and  endLastTime.trim() != "" '>
                    <![CDATA[ and date_format(B.CREATE_TIME,'%Y-%m-%d') >= date_format(#{startLastTime},'%Y-%m-%d')
                and date_format(B.CREATE_TIME,'%Y-%m-%d') <= date_format(#{endLastTime},'%Y-%m-%d') ]]>
                </if>
            </when>
            <otherwise>
                <if test='startTime != null and  startTime.trim() != "" and endTime != null and  endTime.trim() != "" '>
                    <![CDATA[ and to_date(B.CREATE_TIME,'YYYY-MM-DD') >= to_date(#{startTime},'YYYY-MM-DD')
                and to_date(B.CREATE_TIME,'YYYY-MM-DD') <= to_date(#{endTime},'YYYY-MM-DD') ]]>
                </if>
                <if test='startLastTime != null and  startLastTime.trim() != "" and endLastTime != null and  endLastTime.trim() != "" '>
                    <![CDATA[ and to_date(B.CREATE_TIME,'YYYY-MM-DD') >= to_date(#{startLastTime},'YYYY-MM-DD')
                and to_date(B.CREATE_TIME,'YYYY-MM-DD') <= to_date(#{endLastTime},'YYYY-MM-DD') ]]>
                </if>
            </otherwise>
        </choose>

    </sql>

    <!-- save object，保存对象信息 -->
    <insert id="save" parameterType="net.linlan.sys.job.entity.ScheduleInfo">
        INSERT INTO SCHEDULE_INFO
        (
            SCHEDULE_ID, APP_ID, JOB_SOURCE, JOB_TYPE, SCHEDULE_NAME, OBJECT_NAME, METHOD_NAME, PARAMS, CRON_EXPRESSION, JOB_CONFIG, START_DATE, END_DATE, STATUS, CREATOR_ID, CREATE_TIME, LAST_TIME, DESCRIPTION, SPARE1, SPARE2
        )
        VALUES
        (
            #{id}, #{appId}, #{jobSource}, #{jobType}, #{scheduleName}, #{objectName}, #{methodName}, #{params}, #{cronExpression}, #{jobConfig}, #{startDate}, #{endDate}, #{status}, #{creatorId}, #{createTime}, #{lastTime}, #{description}, #{spare1}, #{spare2}
        )
    </insert>

    <!-- update object，更新对象信息 -->
    <update id="update" parameterType="net.linlan.sys.job.entity.ScheduleInfo">
        UPDATE SCHEDULE_INFO
        <set>
            <if test="appId != null">APP_ID = #{appId}, </if>
            <if test="jobSource != null">JOB_SOURCE = #{jobSource}, </if>
            <if test="jobType != null">JOB_TYPE = #{jobType}, </if>
            <if test="scheduleName != null">SCHEDULE_NAME = #{scheduleName}, </if>
            <if test="objectName != null">OBJECT_NAME = #{objectName}, </if>
            <if test="methodName != null">METHOD_NAME = #{methodName}, </if>
            <if test="params != null">PARAMS = #{params}, </if>
            <if test="cronExpression != null">CRON_EXPRESSION = #{cronExpression}, </if>
            <if test="jobConfig != null">JOB_CONFIG = #{jobConfig}, </if>
            <if test="startDate != null">START_DATE = #{startDate}, </if>
            <if test="endDate != null">END_DATE = #{endDate}, </if>
            <if test="status != null">STATUS = #{status}, </if>
            <if test="lastTime != null">LAST_TIME = #{lastTime}, </if>
            <if test="description != null">DESCRIPTION = #{description}, </if>
            <if test="spare1 != null">SPARE1 = #{spare1}, </if>
            <if test="spare2 != null">SPARE2 = #{spare2}</if>
        </set>
        WHERE SCHEDULE_ID = #{id}
    </update>

    <!-- delete by id，通过主键删除对象 -->
    <delete id="deleteById">
        DELETE FROM SCHEDULE_INFO
        WHERE SCHEDULE_ID = #{value}
    </delete>

    <!-- delete by ids，通过主键数组批量删除对象 -->
    <delete id="deleteByIds">
        DELETE FROM SCHEDULE_INFO
        WHERE SCHEDULE_ID IN
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- query total，通过输入的条件，查询返回结果的数量，返回值为int -->
    <select id="getCount" resultType="int">
        SELECT COUNT(1)
        FROM SCHEDULE_INFO B
        WHERE 1 = 1
        <include refid="commonList"></include>
    </select>

    <!-- 以下为新增特定的操作 -->
    <!-- get list with dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <resultMap type="net.linlan.sys.job.dto.ScheduleInfoDto" extends="scheduleInfoMap" id="scheduleInfoDtoMap">
        <result property="appName" column="appName"/>
    </resultMap>

    <!-- get page with param input dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <select id="getPageDto" resultMap="scheduleInfoDtoMap">
        SELECT
        B.SCHEDULE_ID, B.APP_ID, B.JOB_SOURCE, B.JOB_TYPE, B.SCHEDULE_NAME, B.OBJECT_NAME, B.METHOD_NAME, B.PARAMS, B.CRON_EXPRESSION, B.JOB_CONFIG, B.START_DATE, B.END_DATE, B.STATUS, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DESCRIPTION, B.SPARE1, B.SPARE2
        , GA.NAME AS appName
        FROM SCHEDULE_INFO B
        <include refid="commonUnionJoin"></include>
        WHERE 1=1
        <include refid="commonList"></include>
        ORDER BY B.CREATE_TIME DESC

    </select>

    <!-- 关联表 -->
    <sql id="commonUnionJoin">
        LEFT JOIN BASE_APP GA ON GA.APP_ID = B.APP_ID
    </sql>

    <!-- get dto with input id，通过ID，查询dto对象 -->
    <select id="getDtoById" resultMap="scheduleInfoDtoMap">
        SELECT
        SCHEDULE_ID, APP_ID, JOB_SOURCE, JOB_TYPE, SCHEDULE_NAME, OBJECT_NAME, METHOD_NAME, PARAMS, CRON_EXPRESSION, JOB_CONFIG, START_DATE, END_DATE, STATUS, CREATOR_ID, CREATE_TIME, LAST_TIME, DESCRIPTION, SPARE1, SPARE2
        FROM SCHEDULE_INFO
        WHERE SCHEDULE_ID = #{value}
    </select>

    <update id="updateStatusBatch">
        update SCHEDULE_INFO
        set status = #{status}
        , LAST_TIME = #{lastTime}
        where SCHEDULE_ID in
        <foreach item="id" collection="ids"  open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
