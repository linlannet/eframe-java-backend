<?xml version="1.0" encoding="UTF-8"?>
<!--

    module-social - 社交平台对接模块
    Copyright © 2020-2025 Linlan (open@linlan.net)

    This program is free software: you can redistribute it and/or modify
    it under the terms of the GNU Affero General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    This program is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU Affero General Public License for more details.

    You should have received a copy of the GNU Affero General Public License
    along with this program.  If not, see <http://www.gnu.org/licenses/>.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.linlan.social.third.dao.ThirdMemberDao">

    <!-- 采用别名使用类成员变量，可根据需求，确定是否启用 -->
    <resultMap type="net.linlan.social.third.entity.ThirdMember" id="thirdMemberMap">
        <id property="id" column="MEMBER_ID"/>
        <result property="userId" column="USER_ID"/>
        <result property="organId" column="ORGAN_ID"/>
        <result property="foreignId" column="FOREIGN_ID"/>
        <result property="accountId" column="ACCOUNT_ID"/>
        <result property="accountType" column="ACCOUNT_TYPE"/>
        <result property="unionId" column="UNION_ID"/>
        <result property="openId" column="OPEN_ID"/>
        <result property="email" column="EMAIL"/>
        <result property="areaCode" column="AREA_CODE"/>
        <result property="mobile" column="MOBILE"/>
        <result property="nickName" column="NICK_NAME"/>
        <result property="avatarUrl" column="AVATAR_URL"/>
        <result property="additionInfo" column="ADDITION_INFO"/>
        <result property="creatorId" column="CREATOR_ID"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="lastTime" column="LAST_TIME"/>
        <result property="delFlag" column="DEL_FLAG"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="spare1" column="SPARE1"/>
        <result property="spare2" column="SPARE2"/>
    </resultMap>

    <!-- find by id，通过主键获取对象 -->
    <select id="findById" resultMap="thirdMemberMap">
        SELECT
MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        FROM THIRD_MEMBER B
        WHERE B.MEMBER_ID = #{value}
    </select>

    <!-- get list，通过输入的条件，获取返回的list列表对象，采用相应的分页规则进行分页处理 -->
    <select id="getList" resultMap="thirdMemberMap">
        SELECT
B.MEMBER_ID, B.USER_ID, B.ORGAN_ID, B.FOREIGN_ID, B.ACCOUNT_ID, B.ACCOUNT_TYPE, B.UNION_ID, B.OPEN_ID, B.EMAIL, B.AREA_CODE, B.MOBILE, B.NICK_NAME, B.AVATAR_URL, B.ADDITION_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM THIRD_MEMBER B
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>
    </select>

    <!-- 通用排序SQL，如果传入参数按照参数排序，如果未传入参数，按照主键排序 -->
    <sql id="getListOrderBy">
        <choose>
            <when test='orderBy != null'>
                <choose>
                    <when test='orderBy == 0'>
                        ORDER BY B.CREATE_TIME DESC
                    </when>
                    <when test='orderBy == 1'>
                        ORDER BY B.CREATE_TIME ASC
                    </when>
                    <when test='orderBy == 2'>
                        ORDER BY B.PRIORITY DESC
                    </when>
                    <when test='orderBy == 3'>
                        ORDER BY B.PRIORITY ASC
                    </when>
                    <otherwise>
                        ORDER BY B.MEMBER_ID ASC
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                ORDER BY B.MEMBER_ID DESC
            </otherwise>
        </choose>
    </sql>

    <!-- sql commonList 通过输入的条件，获取返回的list列表对象 -->
    <sql id="commonList">
        <!--  只允许默认的本表的字段查询，查询条件可通过Param定义，默认全部字段，后续根据Param内的定义，可删除不需要的查询字段 -->
        <if test="id != null and id != ''">
            and B.MEMBER_ID = #{id}
        </if>
        <if test="organId != null and organId != ''">
            and B.ORGAN_ID = #{organId}
        </if>
        <if test="foreignId != null and foreignId != ''">
            and B.FOREIGN_ID = #{foreignId}
        </if>
        <if test="accountId != null and accountId != ''">
            and B.ACCOUNT_ID = #{accountId}
        </if>
        <if test="accountType != null and accountType != ''">
            and B.ACCOUNT_TYPE = #{accountType}
        </if>
        <if test="unionId != null and unionId != ''">
            and B.UNION_ID = #{unionId}
        </if>
        <if test="openId != null and openId != ''">
            and B.OPEN_ID = #{openId}
        </if>
        <if test="email != null and email != ''">
            and B.EMAIL = #{email}
        </if>
        <if test="areaCode != null and areaCode != ''">
            and B.AREA_CODE = #{areaCode}
        </if>
        <if test="mobile != null and mobile != ''">
            and B.MOBILE = #{mobile}
        </if>
        <if test="nickName != null and nickName != ''">
            and B.NICK_NAME = #{nickName}
        </if>
        <if test="avatarUrl != null and avatarUrl != ''">
            and B.AVATAR_URL = #{avatarUrl}
        </if>
        <if test="additionInfo != null and additionInfo != ''">
            and B.ADDITION_INFO = #{additionInfo}
        </if>
        <if test="creatorId != null and creatorId != ''">
            and B.CREATOR_ID = #{creatorId}
        </if>
        <choose>
            <when test="delFlag != null">
                AND B.DEL_FLAG = #{delFlag}
            </when>
            <otherwise>
                AND ( B.DEL_FLAG = 0 OR B.DEL_FLAG IS NULL)
            </otherwise>
        </choose>
        <if test="description != null and description != ''">
            and B.DESCRIPTION = #{description}
        </if>
        <if test="spare1 != null and spare1 != ''">
            and B.SPARE1 = #{spare1}
        </if>
        <if test="spare2 != null and spare2 != ''">
            and B.SPARE2 = #{spare2}
        </if>

        <!-- 各个数据库差异化的查询条件处理，如时间字段 -->
        <choose>
            <when test='_databaseId == "mysql"'>

            </when>
            <when test='_databaseId == "dm8"'>

            </when>
            <otherwise>

            </otherwise>
        </choose>

    </sql>

    <!-- save object，保存对象信息 -->
    <insert id="save" parameterType="net.linlan.social.third.entity.ThirdMember">
        INSERT INTO THIRD_MEMBER
        (
MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        )
        VALUES
        (
#{id}, #{userId}, #{organId}, #{foreignId}, #{accountId}, #{accountType}, #{unionId}, #{openId}, #{email}, #{areaCode}, #{mobile}, #{nickName}, #{avatarUrl}, #{additionInfo}, #{creatorId}, #{createTime}, #{lastTime}, #{delFlag}, #{description}, #{spare1}, #{spare2}
        )
    </insert>

    <insert id="batchSave" parameterType="java.util.List">
        <choose>
            <when test='_databaseId == "mysql"'>
                INSERT INTO THIRD_MEMBER
                (
MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
                )
                VALUES
                <foreach collection="list" index="index" item="item" separator=",">
                    (
#{item.id}, #{item.userId}, #{item.organId}, #{item.foreignId}, #{item.accountId}, #{item.accountType}, #{item.unionId}, #{item.openId}, #{item.email}, #{item.areaCode}, #{item.mobile}, #{item.nickName}, #{item.avatarUrl}, #{item.additionInfo}, #{item.creatorId}, #{item.createTime}, #{item.lastTime}, #{item.delFlag}, #{item.description}, #{item.spare1}, #{item.spare2}
                    )
                </foreach>
            </when>
            <otherwise>
                <foreach collection="list" item="item" index="index" open="begin" close=";end;" separator=";">
                    INSERT INTO THIRD_MEMBER
                    (
MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
                    )
                    VALUES
                    (
#{item.id}, #{item.userId}, #{item.organId}, #{item.foreignId}, #{item.accountId}, #{item.accountType}, #{item.unionId}, #{item.openId}, #{item.email}, #{item.areaCode}, #{item.mobile}, #{item.nickName}, #{item.avatarUrl}, #{item.additionInfo}, #{item.creatorId}, #{item.createTime}, #{item.lastTime}, #{item.delFlag}, #{item.description}, #{item.spare1}, #{item.spare2}
                    )
                </foreach>
            </otherwise>
        </choose>
    </insert>

    <!-- update object，更新对象信息 -->
    <update id="update" parameterType="net.linlan.social.third.entity.ThirdMember">
        UPDATE THIRD_MEMBER
        <set>
            <if test="userId != null">USER_ID = #{userId}, </if>
            <if test="organId != null">ORGAN_ID = #{organId}, </if>
            <if test="foreignId != null">FOREIGN_ID = #{foreignId}, </if>
            <if test="accountId != null">ACCOUNT_ID = #{accountId}, </if>
            <if test="accountType != null">ACCOUNT_TYPE = #{accountType}, </if>
            <if test="unionId != null">UNION_ID = #{unionId}, </if>
            <if test="openId != null">OPEN_ID = #{openId}, </if>
            <if test="email != null">EMAIL = #{email}, </if>
            <if test="areaCode != null">AREA_CODE = #{areaCode}, </if>
            <if test="mobile != null">MOBILE = #{mobile}, </if>
            <if test="nickName != null">NICK_NAME = #{nickName}, </if>
            <if test="avatarUrl != null">AVATAR_URL = #{avatarUrl}, </if>
            <if test="additionInfo != null">ADDITION_INFO = #{additionInfo}, </if>
            <if test="creatorId != null">CREATOR_ID = #{creatorId}, </if>
            <if test="createTime != null">CREATE_TIME = #{createTime}, </if>
            <if test="lastTime != null">LAST_TIME = #{lastTime}, </if>
            <if test="delFlag != null">DEL_FLAG = #{delFlag}, </if>
            <if test="description != null">DESCRIPTION = #{description}, </if>
            <if test="spare1 != null">SPARE1 = #{spare1}, </if>
            <if test="spare2 != null">SPARE2 = #{spare2}</if>
        </set>
        WHERE MEMBER_ID = #{id}
    </update>

    <!-- delete by id，通过主键删除对象 -->
    <delete id="deleteById">
        DELETE FROM THIRD_MEMBER
        WHERE MEMBER_ID = #{value}
    </delete>

    <!-- delete by ids，通过主键数组批量删除对象 -->
    <delete id="deleteByIds">
        DELETE FROM THIRD_MEMBER
        WHERE MEMBER_ID IN
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- query total，通过输入的条件，查询返回结果的数量，返回值为int -->
    <select id="getCount" resultType="int">
        SELECT COUNT(1)
        FROM THIRD_MEMBER B
        WHERE 1 = 1
        <include refid="commonList"></include>
    </select>

    <!-- get list with dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <resultMap type="net.linlan.social.third.dto.ThirdMemberDto" extends="thirdMemberMap" id="thirdMemberDtoMap">

    </resultMap>

    <!-- get page with param input dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <select id="getPageDto" resultMap="thirdMemberDtoMap">
        SELECT
B.MEMBER_ID, B.USER_ID, B.ORGAN_ID, B.FOREIGN_ID, B.ACCOUNT_ID, B.ACCOUNT_TYPE, B.UNION_ID, B.OPEN_ID, B.EMAIL, B.AREA_CODE, B.MOBILE, B.NICK_NAME, B.AVATAR_URL, B.ADDITION_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM THIRD_MEMBER B
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>

    </select>

    <!-- get dto with input id，通过ID，查询dto对象 -->
    <select id="getDtoById" resultMap="thirdMemberDtoMap">
        SELECT
B.MEMBER_ID, B.USER_ID, B.ORGAN_ID, B.FOREIGN_ID, B.ACCOUNT_ID, B.ACCOUNT_TYPE, B.UNION_ID, B.OPEN_ID, B.EMAIL, B.AREA_CODE, B.MOBILE, B.NICK_NAME, B.AVATAR_URL, B.ADDITION_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM THIRD_MEMBER B
        WHERE MEMBER_ID = #{value}
    </select>

    <!-- 以下为新增特定的操作 -->
    <select id="findByUserId" resultMap="thirdMemberMap">
        SELECT
            MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        FROM THIRD_MEMBER B
        WHERE B.USER_ID = #{value}
    </select>

    <select id="findByAccountIdOpenId" resultMap="thirdMemberMap">
        SELECT
            MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        FROM THIRD_MEMBER B
        WHERE B.ACCOUNT_ID = #{accountId} AND B.OPEN_ID = #{openId} AND B.DEL_FLAG = 0
    </select>

    <select id="findByUserIdOpenId" resultMap="thirdMemberMap">
        SELECT
            MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        FROM THIRD_MEMBER B
        WHERE B.USER_ID = #{userId} AND B.OPEN_ID = #{openId} AND B.DEL_FLAG = 0
    </select>

    <select id="findByUserIdAccountId" resultMap="thirdMemberMap">
        SELECT
            MEMBER_ID, USER_ID, ORGAN_ID, FOREIGN_ID, ACCOUNT_ID, ACCOUNT_TYPE, UNION_ID, OPEN_ID, EMAIL, AREA_CODE, MOBILE, NICK_NAME, AVATAR_URL, ADDITION_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        FROM THIRD_MEMBER B
        WHERE B.USER_ID = #{userId} AND B.ACCOUNT_ID = #{accountId} AND B.DEL_FLAG = 0
    </select>

    <select id="getByParams" resultMap="thirdMemberDtoMap">
        select B.* from THIRD_MEMBER B
        WHERE 1 = 1
        <include refid="commonList"></include>
    </select>

    <!-- get list with dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <resultMap type="net.linlan.social.third.dto.ThirdUserDto" extends="thirdMemberMap" id="thirdUserDtoMap">
        <association property="baseUser" column="{value=USER_ID}" select="net.linlan.sys.base.dao.BaseUserDao.findById"></association>
        <association property="baseUserExt" column="{value=USER_ID}" select="net.linlan.sys.base.dao.BaseUserExtDao.findById"></association>
    </resultMap>

    <select id="getThirdUserById" resultMap="thirdUserDtoMap">
        SELECT
            B.MEMBER_ID, B.USER_ID, B.ORGAN_ID, B.FOREIGN_ID, B.ACCOUNT_ID, B.ACCOUNT_TYPE, B.UNION_ID, B.OPEN_ID, B.EMAIL, B.AREA_CODE, B.MOBILE, B.NICK_NAME, B.AVATAR_URL, B.ADDITION_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
            , BU.*, BUE.*
        FROM THIRD_MEMBER B
        LEFT JOIN BASE_USER BU ON B.USER_ID = BU.USER_ID
        LEFT JOIN BASE_USER_EXT BUE ON B.USER_ID = BUE.USER_ID
        WHERE B.MEMBER_ID = #{value}
    </select>

    <select id="getThirdUserByUserId" resultMap="thirdUserDtoMap">
        SELECT
            B.MEMBER_ID, B.USER_ID, B.ORGAN_ID, B.FOREIGN_ID, B.ACCOUNT_ID, B.ACCOUNT_TYPE, B.UNION_ID, B.OPEN_ID, B.EMAIL, B.AREA_CODE, B.MOBILE, B.NICK_NAME, B.AVATAR_URL, B.ADDITION_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
             , BU.*, BUE.*
        FROM THIRD_MEMBER B
                 LEFT JOIN BASE_USER BU ON B.USER_ID = BU.USER_ID
                 LEFT JOIN BASE_USER_EXT BUE ON B.USER_ID = BUE.USER_ID
        WHERE B.USER_ID = #{value}
    </select>

    <select id="getThirdUserByOpenId" resultMap="thirdUserDtoMap">
        SELECT
            B.MEMBER_ID, B.USER_ID, B.ORGAN_ID, B.FOREIGN_ID, B.ACCOUNT_ID, B.ACCOUNT_TYPE, B.UNION_ID, B.OPEN_ID, B.EMAIL, B.AREA_CODE, B.MOBILE, B.NICK_NAME, B.AVATAR_URL, B.ADDITION_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
             , BU.*, BUE.*
        FROM THIRD_MEMBER B
                 LEFT JOIN BASE_USER BU ON B.USER_ID = BU.USER_ID
                 LEFT JOIN BASE_USER_EXT BUE ON B.USER_ID = BUE.USER_ID
        WHERE B.OPEN_ID = #{openId} and B.ACCOUNT_TYPE = #{source}
    </select>

</mapper>
