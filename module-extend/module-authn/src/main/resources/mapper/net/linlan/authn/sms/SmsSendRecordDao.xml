<?xml version="1.0" encoding="UTF-8"?>
<!--

    module-authn - 动态认证对接模块
    Copyright © 2020-2025 Linlan (open@linlan.net)

    This program is free software: you can redistribute it and/or modify
    it under the terms of the GNU Affero General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    This program is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU Affero General Public License for more details.

    You should have received a copy of the GNU Affero General Public License
    along with this program.  If not, see <http://www.gnu.org/licenses/>.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.linlan.authn.sms.dao.SmsSendRecordDao">

    <!-- 采用别名使用类成员变量，可根据需求，确定是否启用 -->
    <resultMap type="net.linlan.authn.sms.entity.SmsSendRecord" id="smsSendRecordMap">
        <id property="id" column="SEND_ID"/>
        <result property="appId" column="APP_ID"/>
        <result property="providerId" column="PROVIDER_ID"/>
        <result property="providerName" column="PROVIDER_NAME"/>
        <result property="foreignId" column="FOREIGN_ID"/>
        <result property="receiverMobile" column="RECEIVER_MOBILE"/>
        <result property="receiverName" column="RECEIVER_NAME"/>
        <result property="bizType" column="BIZ_TYPE"/>
        <result property="bizSubtype" column="BIZ_SUBTYPE"/>
        <result property="content" column="CONTENT"/>
        <result property="msgUrl" column="MSG_URL"/>
        <result property="status" column="STATUS"/>
        <result property="sendTime" column="SEND_TIME"/>
        <result property="sendStatus" column="SEND_STATUS"/>
        <result property="returnTime" column="RETURN_TIME"/>
        <result property="returnInfo" column="RETURN_INFO"/>
        <result property="creatorId" column="CREATOR_ID"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="lastTime" column="LAST_TIME"/>
        <result property="delFlag" column="DEL_FLAG"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="spare1" column="SPARE1"/>
        <result property="spare2" column="SPARE2"/>
    </resultMap>

    <!-- find by id，通过主键获取对象 -->
    <select id="findById" resultMap="smsSendRecordMap">
        SELECT
SEND_ID, APP_ID, PROVIDER_ID, PROVIDER_NAME, FOREIGN_ID, RECEIVER_MOBILE, RECEIVER_NAME, BIZ_TYPE, BIZ_SUBTYPE, CONTENT, MSG_URL, STATUS, SEND_TIME, SEND_STATUS, RETURN_TIME, RETURN_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        FROM PUBLIC_SMS_SEND_RECORD B
        WHERE B.SEND_ID = #{value}
    </select>

    <!-- get list，通过输入的条件，获取返回的list列表对象，采用相应的分页规则进行分页处理 -->
    <select id="getList" resultMap="smsSendRecordMap">
        SELECT
B.SEND_ID, B.APP_ID, B.PROVIDER_ID, B.PROVIDER_NAME, B.FOREIGN_ID, B.RECEIVER_MOBILE, B.RECEIVER_NAME, B.BIZ_TYPE, B.BIZ_SUBTYPE, B.CONTENT, B.MSG_URL, B.STATUS, B.SEND_TIME, B.SEND_STATUS, B.RETURN_TIME, B.RETURN_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM PUBLIC_SMS_SEND_RECORD B
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>
    </select>

    <!-- 通用排序SQL，如果传入参数按照参数排序，如果未传入参数，按照主键排序 -->
    <sql id="getListOrderBy">
        <choose>
            <when test='orderBy != null'>
                <choose>
                    <when test='orderBy == 0'>
                        ORDER BY B.CREATE_TIME DESC
                    </when>
                    <when test='orderBy == 1'>
                        ORDER BY B.CREATE_TIME ASC
                    </when>
                    <when test='orderBy == 2'>
                        ORDER BY B.PRIORITY DESC
                    </when>
                    <when test='orderBy == 3'>
                        ORDER BY B.PRIORITY ASC
                    </when>
                    <otherwise>
                        ORDER BY B.SEND_ID ASC
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                ORDER BY B.SEND_ID DESC
            </otherwise>
        </choose>
    </sql>

    <!-- sql commonList 通过输入的条件，获取返回的list列表对象 -->
    <sql id="commonList">
        <!--  只允许默认的本表的字段查询，查询条件可通过Param定义，默认全部字段，后续根据Param内的定义，可删除不需要的查询字段 -->
        <if test="id != null and id != ''">
            and B.SEND_ID = #{id}
        </if>
        <if test="appId != null and appId != ''">
            and B.APP_ID = #{appId}
        </if>
        <if test="providerId != null and providerId != ''">
            and B.PROVIDER_ID = #{providerId}
        </if>
        <if test="providerName != null and providerName != ''">
            and B.PROVIDER_NAME = #{providerName}
        </if>
        <if test="foreignId != null and foreignId != ''">
            and B.FOREIGN_ID = #{foreignId}
        </if>
        <if test="receiverMobile != null and receiverMobile != ''">
            and B.RECEIVER_MOBILE = #{receiverMobile}
        </if>
        <if test="receiverName != null and receiverName != ''">
            and B.RECEIVER_NAME = #{receiverName}
        </if>
        <if test="bizType != null and bizType != ''">
            and B.BIZ_TYPE = #{bizType}
        </if>
        <if test="bizSubtype != null and bizSubtype != ''">
            and B.BIZ_SUBTYPE = #{bizSubtype}
        </if>
        <if test="content != null and content != ''">
            and B.CONTENT = #{content}
        </if>
        <if test="msgUrl != null and msgUrl != ''">
            and B.MSG_URL = #{msgUrl}
        </if>
        <if test="status != null and status != ''">
            and B.STATUS = #{status}
        </if>
        <if test="sendTime != null and sendTime != ''">
            and B.SEND_TIME = #{sendTime}
        </if>
        <if test="sendStatus != null and sendStatus != ''">
            and B.SEND_STATUS = #{sendStatus}
        </if>
        <if test="returnTime != null and returnTime != ''">
            and B.RETURN_TIME = #{returnTime}
        </if>
        <if test="returnInfo != null and returnInfo != ''">
            and B.RETURN_INFO = #{returnInfo}
        </if>
        <if test="creatorId != null and creatorId != ''">
            and B.CREATOR_ID = #{creatorId}
        </if>
        <choose>
            <when test="delFlag != null">
                AND B.DEL_FLAG = #{delFlag}
            </when>
            <otherwise>
                AND ( B.DEL_FLAG = 0 OR B.DEL_FLAG IS NULL)
            </otherwise>
        </choose>
        <if test="description != null and description != ''">
            and B.DESCRIPTION = #{description}
        </if>
        <if test="spare1 != null and spare1 != ''">
            and B.SPARE1 = #{spare1}
        </if>
        <if test="spare2 != null and spare2 != ''">
            and B.SPARE2 = #{spare2}
        </if>

        <!-- 各个数据库差异化的查询条件处理，如时间字段 -->
        <choose>
            <when test='_databaseId == "mysql"'>

            </when>
            <when test='_databaseId == "dm8"'>

            </when>
            <otherwise>

            </otherwise>
        </choose>

    </sql>

    <!-- save object，保存对象信息 -->
    <insert id="save" parameterType="net.linlan.authn.sms.entity.SmsSendRecord">
        INSERT INTO PUBLIC_SMS_SEND_RECORD
        (
SEND_ID, APP_ID, PROVIDER_ID, PROVIDER_NAME, FOREIGN_ID, RECEIVER_MOBILE, RECEIVER_NAME, BIZ_TYPE, BIZ_SUBTYPE, CONTENT, MSG_URL, STATUS, SEND_TIME, SEND_STATUS, RETURN_TIME, RETURN_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
        )
        VALUES
        (
#{id}, #{appId}, #{providerId}, #{providerName}, #{foreignId}, #{receiverMobile}, #{receiverName}, #{bizType}, #{bizSubtype}, #{content}, #{msgUrl}, #{status}, #{sendTime}, #{sendStatus}, #{returnTime}, #{returnInfo}, #{creatorId}, #{createTime}, #{lastTime}, #{delFlag}, #{description}, #{spare1}, #{spare2}
        )
    </insert>

    <insert id="batchSave" parameterType="java.util.List">
        <choose>
            <when test='_databaseId == "mysql"'>
                INSERT INTO PUBLIC_SMS_SEND_RECORD
                (
SEND_ID, APP_ID, PROVIDER_ID, PROVIDER_NAME, FOREIGN_ID, RECEIVER_MOBILE, RECEIVER_NAME, BIZ_TYPE, BIZ_SUBTYPE, CONTENT, MSG_URL, STATUS, SEND_TIME, SEND_STATUS, RETURN_TIME, RETURN_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
                )
                VALUES
                <foreach collection="list" index="index" item="item" separator=",">
                    (
#{item.id}, #{item.appId}, #{item.providerId}, #{item.providerName}, #{item.foreignId}, #{item.receiverMobile}, #{item.receiverName}, #{item.bizType}, #{item.bizSubtype}, #{item.content}, #{item.msgUrl}, #{item.status}, #{item.sendTime}, #{item.sendStatus}, #{item.returnTime}, #{item.returnInfo}, #{item.creatorId}, #{item.createTime}, #{item.lastTime}, #{item.delFlag}, #{item.description}, #{item.spare1}, #{item.spare2}
                    )
                </foreach>
            </when>
            <otherwise>
                <foreach collection="list" item="item" index="index" open="begin" close=";end;" separator=";">
                    INSERT INTO PUBLIC_SMS_SEND_RECORD
                    (
SEND_ID, APP_ID, PROVIDER_ID, PROVIDER_NAME, FOREIGN_ID, RECEIVER_MOBILE, RECEIVER_NAME, BIZ_TYPE, BIZ_SUBTYPE, CONTENT, MSG_URL, STATUS, SEND_TIME, SEND_STATUS, RETURN_TIME, RETURN_INFO, CREATOR_ID, CREATE_TIME, LAST_TIME, DEL_FLAG, DESCRIPTION, SPARE1, SPARE2
                    )
                    VALUES
                    (
#{item.id}, #{item.appId}, #{item.providerId}, #{item.providerName}, #{item.foreignId}, #{item.receiverMobile}, #{item.receiverName}, #{item.bizType}, #{item.bizSubtype}, #{item.content}, #{item.msgUrl}, #{item.status}, #{item.sendTime}, #{item.sendStatus}, #{item.returnTime}, #{item.returnInfo}, #{item.creatorId}, #{item.createTime}, #{item.lastTime}, #{item.delFlag}, #{item.description}, #{item.spare1}, #{item.spare2}
                    )
                </foreach>
            </otherwise>
        </choose>
    </insert>

    <!-- update object，更新对象信息 -->
    <update id="update" parameterType="net.linlan.authn.sms.entity.SmsSendRecord">
        UPDATE PUBLIC_SMS_SEND_RECORD
        <set>
            <if test="appId != null">APP_ID = #{appId}, </if>
            <if test="providerId != null">PROVIDER_ID = #{providerId}, </if>
            <if test="providerName != null">PROVIDER_NAME = #{providerName}, </if>
            <if test="foreignId != null">FOREIGN_ID = #{foreignId}, </if>
            <if test="receiverMobile != null">RECEIVER_MOBILE = #{receiverMobile}, </if>
            <if test="receiverName != null">RECEIVER_NAME = #{receiverName}, </if>
            <if test="bizType != null">BIZ_TYPE = #{bizType}, </if>
            <if test="bizSubtype != null">BIZ_SUBTYPE = #{bizSubtype}, </if>
            <if test="content != null">CONTENT = #{content}, </if>
            <if test="msgUrl != null">MSG_URL = #{msgUrl}, </if>
            <if test="status != null">STATUS = #{status}, </if>
            <if test="sendTime != null">SEND_TIME = #{sendTime}, </if>
            <if test="sendStatus != null">SEND_STATUS = #{sendStatus}, </if>
            <if test="returnTime != null">RETURN_TIME = #{returnTime}, </if>
            <if test="returnInfo != null">RETURN_INFO = #{returnInfo}, </if>
            <if test="creatorId != null">CREATOR_ID = #{creatorId}, </if>
            <if test="createTime != null">CREATE_TIME = #{createTime}, </if>
            <if test="lastTime != null">LAST_TIME = #{lastTime}, </if>
            <if test="delFlag != null">DEL_FLAG = #{delFlag}, </if>
            <if test="description != null">DESCRIPTION = #{description}, </if>
            <if test="spare1 != null">SPARE1 = #{spare1}, </if>
            <if test="spare2 != null">SPARE2 = #{spare2}</if>
        </set>
        WHERE SEND_ID = #{id}
    </update>

    <!-- delete by id，通过主键删除对象 -->
    <delete id="deleteById">
        DELETE FROM PUBLIC_SMS_SEND_RECORD
        WHERE SEND_ID = #{value}
    </delete>

    <!-- delete by ids，通过主键数组批量删除对象 -->
    <delete id="deleteByIds">
        DELETE FROM PUBLIC_SMS_SEND_RECORD
        WHERE SEND_ID IN
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- query total，通过输入的条件，查询返回结果的数量，返回值为int -->
    <select id="getCount" resultType="int">
        SELECT COUNT(1)
        FROM PUBLIC_SMS_SEND_RECORD B
        WHERE 1 = 1
        <include refid="commonList"></include>
    </select>

    <!-- get list with dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <resultMap type="net.linlan.authn.sms.dto.SmsSendRecordDto" extends="smsSendRecordMap" id="smsSendRecordDtoMap">

    </resultMap>

    <!-- get page with param input dto result，通过输入的条件，返回查询结果信息，返回值为dto对象 -->
    <select id="getPageDto" resultMap="smsSendRecordDtoMap">
        SELECT
B.SEND_ID, B.APP_ID, B.PROVIDER_ID, B.PROVIDER_NAME, B.FOREIGN_ID, B.RECEIVER_MOBILE, B.RECEIVER_NAME, B.BIZ_TYPE, B.BIZ_SUBTYPE, B.CONTENT, B.MSG_URL, B.STATUS, B.SEND_TIME, B.SEND_STATUS, B.RETURN_TIME, B.RETURN_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM PUBLIC_SMS_SEND_RECORD B
        WHERE 1=1
        <include refid="commonList"></include>
        <include refid="getListOrderBy"></include>

    </select>

    <!-- get dto with input id，通过ID，查询dto对象 -->
    <select id="getDtoById" resultMap="smsSendRecordDtoMap">
        SELECT
B.SEND_ID, B.APP_ID, B.PROVIDER_ID, B.PROVIDER_NAME, B.FOREIGN_ID, B.RECEIVER_MOBILE, B.RECEIVER_NAME, B.BIZ_TYPE, B.BIZ_SUBTYPE, B.CONTENT, B.MSG_URL, B.STATUS, B.SEND_TIME, B.SEND_STATUS, B.RETURN_TIME, B.RETURN_INFO, B.CREATOR_ID, B.CREATE_TIME, B.LAST_TIME, B.DEL_FLAG, B.DESCRIPTION, B.SPARE1, B.SPARE2
        FROM PUBLIC_SMS_SEND_RECORD B
        WHERE SEND_ID = #{value}
    </select>

    <!-- 以下为新增特定的操作 -->


</mapper>
